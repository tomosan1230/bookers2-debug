<% if user_signed_in? %>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field (:word) %>
    <%= f.select :range , options_for_select({ "User" => "", "Book" => "1" }) %>
    <%= f.select :search, options_for_select({ "完全一致" => "perfect", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partial" }) %>
    <%= submit_tag "検索" %>
  <% end %>
<% end %>